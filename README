Для начала установим все нужные библиотеки с помощью команды pip install -r requrements
Потом обратимся к alembic для того, чтобы пользоваться миграциями alembic init <название папки с миграциями>
Перед этим нужно сменить пароли и логины к бд
Потом запустить проект с помощью команды uvicorn main:app --reload    
